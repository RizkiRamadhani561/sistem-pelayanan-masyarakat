[phases.setup]
nixPkgs = ["...", "php82", "php82Packages.composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "npm install"]

[phases.build]
cmds = ["npm run build 2>/dev/null || true"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
