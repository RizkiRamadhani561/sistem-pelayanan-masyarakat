[build]
builder = "PHP"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[build.runtime]
phpVersion = "8.2"

[databases]
# Railway will auto-detect and create database
