[build]
  publish = "public"
  command = "composer install && npm install"

[build.environment]
  PHP_VERSION = "8.2"

# Note: Netlify has limited PHP support. Consider using Railway or DigitalOcean for full PHP apps
